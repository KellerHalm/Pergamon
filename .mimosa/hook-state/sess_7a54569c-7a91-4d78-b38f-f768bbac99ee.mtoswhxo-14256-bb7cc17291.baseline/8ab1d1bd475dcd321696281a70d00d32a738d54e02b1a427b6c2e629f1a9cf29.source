import { e as escape_html, d as ensure_array_like } from "../../../chunks/index.js";
import { P as PosterCard } from "../../../chunks/PosterCard.js";
import { p as peopleApi } from "../../../chunks/api.js";
import { e as roleLabel, d as displayName } from "../../../chunks/constants.js";
import { P as Plus } from "../../../chunks/plus.js";
function _page($$renderer, $$props) {
  $$renderer.component(($$renderer2) => {
    let people = [];
    let loading = true;
    let sort = "created";
    async function load() {
      loading = true;
      try {
        people = await peopleApi.list(sort) || [];
      } catch (e) {
        console.error(e);
        people = [];
      }
      loading = false;
    }
    function setSort(v) {
      sort = v;
      load();
    }
    const sortOptions = [
      { id: "created", label: "По добавлению" },
      { id: "updated", label: "По обновлению" },
      { id: "name", label: "По алфавиту" }
    ];
    function countLabel(n) {
      const m10 = n % 10;
      const m100 = n % 100;
      if (m10 === 1 && m100 !== 11) return `${n} деятель`;
      if (m10 >= 2 && m10 <= 4 && (m100 < 12 || m100 > 14)) return `${n} деятеля`;
      return `${n} деятелей`;
    }
    $$renderer2.push(`<div class="page svelte-1403ju0"><div class="topbar svelte-1403ju0"><h1 class="svelte-1403ju0">Деятели</h1> <div class="count svelte-1403ju0">${escape_html(countLabel(people.length))}</div></div> <div class="toolbar svelte-1403ju0">`);
    $$renderer2.select(
      {
        class: "select sort",
        value: sort,
        onchange: (e) => setSort(e.currentTarget.value)
      },
      ($$renderer3) => {
        $$renderer3.push(`<!--[-->`);
        const each_array = ensure_array_like(sortOptions);
        for (let $$index = 0, $$length = each_array.length; $$index < $$length; $$index++) {
          let o = each_array[$$index];
          $$renderer3.option({ value: o.id }, ($$renderer4) => {
            $$renderer4.push(`${escape_html(o.label)}`);
          });
        }
        $$renderer3.push(`<!--]-->`);
      },
      "svelte-1403ju0"
    );
    $$renderer2.push(` <a href="/person/add" class="btn btn-primary">`);
    Plus($$renderer2, { size: 16 });
    $$renderer2.push(`<!----> Добавить деятеля</a></div> `);
    if (loading) {
      $$renderer2.push("<!--[0-->");
      $$renderer2.push(`<div class="empty svelte-1403ju0">Загрузка…</div>`);
    } else if (people.length === 0) {
      $$renderer2.push("<!--[1-->");
      $$renderer2.push(`<div class="empty svelte-1403ju0"><div class="empty-icon svelte-1403ju0">✍️</div> <h2 class="svelte-1403ju0">Деятелей пока нет</h2> <p class="svelte-1403ju0">Создайте первого деятеля — автора, художника или режиссёра.</p> <a href="/person/add" class="btn btn-primary">Добавить деятеля</a></div>`);
    } else {
      $$renderer2.push("<!--[-1-->");
      $$renderer2.push(`<div class="grid svelte-1403ju0"><!--[-->`);
      const each_array_1 = ensure_array_like(people);
      for (let $$index_1 = 0, $$length = each_array_1.length; $$index_1 < $$length; $$index_1++) {
        let p = each_array_1[$$index_1];
        PosterCard($$renderer2, {
          href: `/person/${p.id}`,
          image: p.mainImage,
          title: displayName(p.names),
          subtitle: roleLabel(p.role)
        });
      }
      $$renderer2.push(`<!--]--></div>`);
    }
    $$renderer2.push(`<!--]--></div>`);
  });
}
export {
  _page as default
};
