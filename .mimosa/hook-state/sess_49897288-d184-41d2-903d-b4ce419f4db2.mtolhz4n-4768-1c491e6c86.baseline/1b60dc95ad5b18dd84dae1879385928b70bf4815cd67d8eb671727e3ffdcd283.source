import "clsx";
import { e as escape_html, f as attr, d as ensure_array_like } from "../../../../chunks/index.js";
import "@sveltejs/kit/internal";
import "../../../../chunks/exports.js";
import "../../../../chunks/utils2.js";
import "@sveltejs/kit/internal/server";
import "../../../../chunks/root.js";
import "../../../../chunks/state.svelte.js";
import { d as displayName, N as NAME_KINDS } from "../../../../chunks/constants.js";
/* empty css                                                          */
import { I as Image_plus, L as Link, M as Minus, C as Chevron_left, a as Chevron_right, X } from "../../../../chunks/chevron-right.js";
import { P as Plus } from "../../../../chunks/plus.js";
function StudioForm($$renderer, $$props) {
  $$renderer.component(($$renderer2) => {
    let names = [{ kind: "original", value: "" }];
    let mainUploading = false;
    let mainUrlInput = "";
    let founded = "";
    let founders = [];
    let description = "";
    let images = [];
    let imageUrls = {};
    let titleIds = [];
    let allTitles = [];
    let saving = false;
    function titleCandidates() {
      return allTitles.filter((t) => !titleIds.includes(t.id));
    }
    function pickTitle(e) {
      const v = Number(e.currentTarget.value);
      if (v > 0 && !titleIds.includes(v)) {
        titleIds.push(v);
      }
    }
    {
      $$renderer2.push("<!--[-1-->");
    }
    $$renderer2.push(`<!--]--> <div class="form svelte-18glnws"><div class="field svelte-18glnws"><span class="label svelte-18glnws">Main изображение</span> <div class="cover-area svelte-18glnws"><div class="cover-preview svelte-18glnws">`);
    {
      $$renderer2.push("<!--[-1-->");
      $$renderer2.push(`<div class="cover-placeholder svelte-18glnws">`);
      Image_plus($$renderer2, { size: 32 });
      $$renderer2.push(`<!----></div>`);
    }
    $$renderer2.push(`<!--]--></div> <div class="cover-actions svelte-18glnws"><label class="btn">`);
    Image_plus($$renderer2, { size: 14 });
    $$renderer2.push(`<!----> ${escape_html("С устройства")} <input type="file" accept="image/*" hidden=""/></label> <div class="row-2 svelte-18glnws"><input class="input svelte-18glnws" placeholder="Ссылка на изображение…"${attr("value", mainUrlInput)}/> <button class="btn btn-primary sm svelte-18glnws"${attr("disabled", mainUploading, true)}>`);
    Link($$renderer2, { size: 14 });
    $$renderer2.push(`<!----></button></div></div></div></div> <div class="field svelte-18glnws"><span class="label svelte-18glnws">Имена</span> <!--[-->`);
    const each_array = ensure_array_like(names);
    for (let i = 0, $$length = each_array.length; i < $$length; i++) {
      let n = each_array[i];
      $$renderer2.push(`<div class="row-2 svelte-18glnws">`);
      $$renderer2.select(
        { class: "select sm", value: n.kind },
        ($$renderer3) => {
          $$renderer3.push(`<!--[-->`);
          const each_array_1 = ensure_array_like(NAME_KINDS);
          for (let $$index = 0, $$length2 = each_array_1.length; $$index < $$length2; $$index++) {
            let k = each_array_1[$$index];
            $$renderer3.option({ value: k.id }, ($$renderer4) => {
              $$renderer4.push(`${escape_html(k.label)}`);
            });
          }
          $$renderer3.push(`<!--]-->`);
        },
        "svelte-18glnws"
      );
      $$renderer2.push(` <input class="input svelte-18glnws" placeholder="Имя…"${attr("value", n.value)}/> `);
      if (names.length > 1) {
        $$renderer2.push("<!--[0-->");
        $$renderer2.push(`<button class="btn btn-icon sm svelte-18glnws">`);
        Minus($$renderer2, { size: 14 });
        $$renderer2.push(`<!----></button>`);
      } else {
        $$renderer2.push("<!--[-1-->");
      }
      $$renderer2.push(`<!--]--></div>`);
    }
    $$renderer2.push(`<!--]--> <button class="btn sm svelte-18glnws">`);
    Plus($$renderer2, { size: 14 });
    $$renderer2.push(`<!----> Добавить имя</button></div> <div class="field svelte-18glnws"><span class="label svelte-18glnws">Дата создания</span> <input class="input" placeholder="Например: 1986 или 1986-12-24…"${attr("value", founded)}/></div> <div class="field svelte-18glnws"><span class="label svelte-18glnws">Основатели</span> <!--[-->`);
    const each_array_2 = ensure_array_like(founders);
    for (let i = 0, $$length = each_array_2.length; i < $$length; i++) {
      each_array_2[i];
      $$renderer2.push(`<div class="row-2 svelte-18glnws"><input class="input svelte-18glnws" placeholder="Имя основателя…"${attr("value", founders[i])}/> <button class="btn btn-icon sm svelte-18glnws">`);
      Minus($$renderer2, { size: 14 });
      $$renderer2.push(`<!----></button></div>`);
    }
    $$renderer2.push(`<!--]--> <button class="btn sm svelte-18glnws">`);
    Plus($$renderer2, { size: 14 });
    $$renderer2.push(`<!----> Добавить основателя</button></div> <div class="field svelte-18glnws"><span class="label svelte-18glnws">Описание</span> <textarea class="textarea"${attr("rows", 4)} placeholder="История студии…">`);
    const $$body = escape_html(description);
    if ($$body) {
      $$renderer2.push(`${$$body}`);
    }
    $$renderer2.push(`</textarea></div> <div class="field svelte-18glnws"><span class="label svelte-18glnws">Тайтлы</span> `);
    if (titleIds.length > 0) {
      $$renderer2.push("<!--[0-->");
      $$renderer2.push(`<!--[-->`);
      const each_array_3 = ensure_array_like(titleIds);
      for (let i = 0, $$length = each_array_3.length; i < $$length; i++) {
        let tid = each_array_3[i];
        $$renderer2.push(`<div class="row-2 svelte-18glnws"><input class="input svelte-18glnws" disabled=""${attr("value", displayName(allTitles.find((t) => t.id === tid)?.names || []))}/> <button class="btn btn-icon sm svelte-18glnws">`);
        Minus($$renderer2, { size: 14 });
        $$renderer2.push(`<!----></button></div>`);
      }
      $$renderer2.push(`<!--]-->`);
    } else {
      $$renderer2.push("<!--[-1-->");
      $$renderer2.push(`<span class="hint svelte-18glnws">Студия пока не привязана ни к одному тайтлу.</span>`);
    }
    $$renderer2.push(`<!--]--> `);
    if (titleCandidates().length > 0) {
      $$renderer2.push("<!--[0-->");
      $$renderer2.push(`<div class="row-2 svelte-18glnws">`);
      $$renderer2.select(
        { class: "select sm", value: 0, onchange: pickTitle },
        ($$renderer3) => {
          $$renderer3.option({ value: 0, disabled: true, hidden: true }, ($$renderer4) => {
            $$renderer4.push(`Добавить в тайтл…`);
          });
          $$renderer3.push(`<!--[-->`);
          const each_array_4 = ensure_array_like(titleCandidates());
          for (let $$index_4 = 0, $$length = each_array_4.length; $$index_4 < $$length; $$index_4++) {
            let t = each_array_4[$$index_4];
            $$renderer3.option({ value: t.id }, ($$renderer4) => {
              $$renderer4.push(`${escape_html(displayName(t.names))}`);
            });
          }
          $$renderer3.push(`<!--]-->`);
        },
        "svelte-18glnws"
      );
      $$renderer2.push(`</div>`);
    } else {
      $$renderer2.push("<!--[-1-->");
    }
    $$renderer2.push(`<!--]--></div> <div class="field svelte-18glnws"><span class="label svelte-18glnws">Дополнительные изображения</span> `);
    if (images.length > 0) {
      $$renderer2.push("<!--[0-->");
      $$renderer2.push(`<div class="gallery-edit svelte-18glnws"><!--[-->`);
      const each_array_5 = ensure_array_like(images);
      for (let i = 0, $$length = each_array_5.length; i < $$length; i++) {
        let f = each_array_5[i];
        $$renderer2.push(`<div class="gallery-thumb svelte-18glnws"><img${attr("src", imageUrls[f])} alt="" class="svelte-18glnws"/> <div class="thumb-actions svelte-18glnws">`);
        if (i > 0) {
          $$renderer2.push("<!--[0-->");
          $$renderer2.push(`<button class="btn btn-icon sm svelte-18glnws">`);
          Chevron_left($$renderer2, { size: 13 });
          $$renderer2.push(`<!----></button>`);
        } else {
          $$renderer2.push("<!--[-1-->");
        }
        $$renderer2.push(`<!--]--> `);
        if (i < images.length - 1) {
          $$renderer2.push("<!--[0-->");
          $$renderer2.push(`<button class="btn btn-icon sm svelte-18glnws">`);
          Chevron_right($$renderer2, { size: 13 });
          $$renderer2.push(`<!----></button>`);
        } else {
          $$renderer2.push("<!--[-1-->");
        }
        $$renderer2.push(`<!--]--> <button class="btn btn-icon sm svelte-18glnws">`);
        X($$renderer2, { size: 13 });
        $$renderer2.push(`<!----></button></div></div>`);
      }
      $$renderer2.push(`<!--]--></div>`);
    } else {
      $$renderer2.push("<!--[-1-->");
    }
    $$renderer2.push(`<!--]--> <label class="btn sm svelte-18glnws">`);
    Image_plus($$renderer2, { size: 14 });
    $$renderer2.push(`<!----> ${escape_html("Добавить изображения")} <input type="file" accept="image/*" multiple="" hidden=""/></label></div> <div class="actions svelte-18glnws"><button class="btn">Отмена</button> <button class="btn btn-primary"${attr("disabled", saving, true)}>${escape_html("Сохранить")}</button></div></div>`);
  });
}
function _page($$renderer) {
  $$renderer.push(`<div class="page svelte-1izhtlf"><h1 class="svelte-1izhtlf">Добавить студию</h1> `);
  StudioForm($$renderer);
  $$renderer.push(`<!----></div>`);
}
export {
  _page as default
};
